const saved=localStorage.getItem('thaer-theme');if(saved==='dark')document.body.classList.add('manual-dark');document.getElementById('themeBtn')?.addEventListener('click',()=>{const b=document.body;b.classList.toggle('manual-dark');localStorage.setItem('thaer-theme',b.classList.contains('manual-dark')?'dark':'light')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelectorAll('.magnetic').forEach(el=>el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`}),false);document.querySelectorAll('.magnetic').forEach(el=>el.addEventListener('mouseleave',()=>el.style.transform=''));